#ifndef _FUZZY_DEFINE_
#define _FUZZY_DEFINE_ 
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <cstring>
#include <vector>
#include <iostream>
#include <fstream>
#include <istream>
#include <ostream>
#include <sstream>
#include <iterator>

#define FZ_MAX_NAME 101
#define FZ_MAX_LIMIT 1
#define FZ_MAX_OUTPUT 0 
#define FZ_MAX_LINE 1024 
#define minimum(a,b) a<b?a:b
#define maximum(a,b) a>b?a:b

enum LogicConnector {NONE, OR, AND};

#endif
